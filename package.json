{
  "name": "connect-four",
  "version": "1.0.0",
  "description": "a basic connect four game",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "cd client/ && yarn install && yarn build"
  },
  "keywords": ["game", "connect four", "fun"],
  "author": "Andy Lindsay",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
