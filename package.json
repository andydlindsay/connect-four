{
  "name": "connect-four",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "cd client/ && npm install && npm run build"
  },
  "keywords": ["game", "connect four", "fun"],
  "author": "Andy Lindsay",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
